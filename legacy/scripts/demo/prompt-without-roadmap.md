You are Claude Code working inside a git repo.

This repo has room for improvement. Look around, pick 3 things worth
improving (bugs, missing tests, docs, small refactors — your call), and
implement them.

Run the tests when you finish: `cd roadmap-skill && node --test test/*.test.js`.

When done, stop. Do not commit.
